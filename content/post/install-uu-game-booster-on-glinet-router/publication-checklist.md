# Publication checklist

- [ ] Confirm the article renders at `/post/install-uu-game-booster-on-glinet-router/`.
- [ ] Confirm `uu-game-booster-glinet-router.svg` displays as the feature/share image.
- [ ] Confirm code blocks render correctly.
- [ ] Optional: replace the generated SVG with a custom hero image.
- [ ] Add affiliate links to the hardware section if desired.
- [ ] Test `hugo --minify` before publishing.
- [ ] Share using the text in `social/`.

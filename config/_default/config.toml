# set `baseurl` to your root domain
# if you set it to "/" share icons won't work properly on production
baseurl = "https://techrelay.pro/"  # Include trailing slash
# title = "Clarity"  # Edit directly from config/_default/languages.toml # alternatively, uncomment this and remove `title` entry from the aforemention file.
copyright = "Tech Relay "
# canonifyurls = true
paginate = 10
theme = ["hugo-embed-pdf-shortcode", "hugo-clarity"]
#disqusShortname = ""
enableInlineShortcodes = true

DefaultContentLanguage = "en"
# [languages]
#config/_default/languages.toml
#disableLanguages = ['pt']

# [menus]
# config/_default/menus/menu.xx.toml

[taxonomies]
category = "categories"
tag = "tags"
series = "series"

[outputs]
home = ["HTML", "RSS", "JSON"]

[params]
google_fonts = [
    ["Fira Code", "400, 700"],
    ["Open Sans", "400, 400i, 700, 700i"]
  ]

  heading_font = "Fira Code"
  body_font = "Open Sans"